[commands.package_test]
description = "Comprehensive Test Creation Instructions for a Specific Java Package in a Projects Using Spock Framework"

arguments = [
    {name = "package", type = "string", required = true, description = "The java package name to create tests for, e.g., com.company.project.service"}
]

instructions = """
# Comprehensive Test Creation Instructions for Java Projects Using Spock Framework

## Overview
These instructions guide the systematic creation of tests for Java projects without existing test coverage, using the Spock Framework for behavior-driven testing in Groovy. The process leverages the scout-server MCP tools for building, testing, and coverage reporting.

**IMPORTANT**: These instructions should be applied EXCLUSIVELY to create tests for the package specified in the `{package_name}` argument. Focus your test creation efforts ONLY on classes within the provided package name (e.g., if {package_name} is "com.company.project.service", create tests ONLY for classes in that specific package, not for any other packages).

## Critical Tool Usage Instructions

### ⚠️ MANDATORY: Primary Tool Selection Guidelines ⚠️

**For Jacoco code coverage and unit testing, scout-server tools are the PRIMARY and PREFERRED choice.** However, other tools may be used for specific use cases as outlined below:

### Primary Tools (Use These First):

- **For running unit tests and Jacoco coverage**:
  - ✅ PRIMARY: `gradle_tester` tool - Use for standard test execution in place of this shell command ./gradlew check
  - ✅ PRIMARY: `gradle_jacoco_coverage_reporter` tool - Use for coverage analysis in place of this shell command ./gradlew jacocoTestReport

### Alternative Tools (For Specific Use Cases):

Other tools may be appropriate for:
- **Integration testing** with external services
- **Performance testing** and benchmarking
- **Mutation testing** tools
- **Static code analysis** beyond coverage
- **Specialized testing frameworks** (e.g., Testcontainers, WireMock)
- **Custom build scripts** or non-Gradle projects

### Tool Selection Priority:
1. **First Choice**: Always try scout-server tools for unit tests and Jacoco coverage
2. **Second Choice**: Use alternative tools only when scout-server tools don't meet specific requirements
3. **Document Why**: When using alternative tools, document the specific use case that requires them

### Example of Correct Tool Usage:
```
# PRIMARY - Using scout-server tools for unit tests and coverage:
<function_calls>
<invoke name="gradle_tester">
<parameter name="projectRoot">/path/to/project</parameter>
</invoke>
</function_calls>

<function_results>OK</function_results>

# ALTERNATIVE - When specific use case requires different tool:
# Document why: Need to run performance benchmarks
# Use appropriate alternative tool with justification
```

## Required MCP Server Tools
- **scout-server**: Provides specialized tools for Gradle projects
  - `gradle_tester`: Executes Gradle build and check tasks
  - `gradle_jacoco_coverage_reporter`: Runs tests and generates JaCoCo coverage reports in XML format
  - `build_gradle_project`: Builds the project (use only when needed for compilation)
  - `dependencies_list`: Lists project dependencies
  - `find_class_usage`: Finds usage of specific classes
  - `get_project_file_info`: Gets information about project files
  - `replace_source_file_contents`: Updates source file contents

## Phase 1: ANALYSIS PHASE

### Objectives
- Map the existing package {package_name}
- Identify testing priorities based on business criticality
- Document technical dependencies
- **Analyze existing tests to understand established patterns**

### Steps
1. **Codebase Scanning**
   - Identify ONLY the package {package_name} and its classes
   - Map class relationships and dependencies WITHIN {package_name}
   - Document external dependencies used by classes in {package_name}
   - Identify entry points WITHIN {package_name} (controllers, main methods, scheduled tasks)
   - **Document existing package structure for {package_name} for mirror test organization**
   - **Catalog existing test files for {package_name} and their coverage**

2. **Initial Coverage Assessment**
   ```
   # ✅ PRIMARY - Use scout-server for Jacoco coverage:
   invoke gradle_jacoco_coverage_reporter with:
   - projectRoot: "/absolute/path/to/project"
   - session_id: "unique-session-id"

   # For non-Gradle projects or specialized coverage tools,
   # document the specific requirement and use appropriate alternative
   ```
   - Review the coverage report from scout-server FOR {package_name} ONLY
   - Identify if {package_name} has zero coverage
   - Check if {package_name} meets its target coverage threshold based on package type:
     - If {package_name} ends with .domain: 90% target (critical business logic)
     - If {package_name} ends with .service: 80% target (orchestration layer)
     - If {package_name} ends with .repository: 70% target (data access layer)
     - If {package_name} ends with .controller: 75% target (API layer)
     - If {package_name} ends with .util: 85% target (utility functions)

3. **Existing Test Analysis**
   - **Identify Existing Tests**: Scan for any existing Spec files FOR {package_name} ONLY
   - **Pattern Recognition**: Document testing patterns already in use for {package_name}
   - **Coverage Gaps**: Identify which classes in {package_name} have tests and which don't
   - **Quality Assessment**: Evaluate existing test quality and completeness for {package_name}
   - **Style Guide Extraction**: Note naming conventions, structure preferences used in {package_name} tests

4. **Component Classification**
   - **Core Business Logic**: Pure functions, domain models, calculators IN {package_name}
   - **Infrastructure Code**: Database repositories, external API clients IN {package_name}
   - **Orchestration Layer**: Services that coordinate multiple components IN {package_name}
   - **Utilities**: Helper classes, common utilities IN {package_name}

5. **Risk Assessment**
   - High-risk components IN {package_name} (payment processing, data validation, security)
   - High-usage components IN {package_name} (called by many other components)
   - Complex components IN {package_name} (high cyclomatic complexity)
   - Components IN {package_name} with frequent changes (check git history)

## Phase 2: PLANNING PHASE

### Objectives
- Design comprehensive test scenarios
- Plan test data strategies
- Define mock boundaries
- **Align with existing test patterns**

### Steps
1. **Test Case Design per Component**
   For each component IN {package_name}, document:
   - Happy path scenarios
   - Edge cases (null, empty, boundary values)
   - Error scenarios (exceptions, invalid inputs)
   - Integration points requiring mocks
   - Performance-sensitive operations
   - **Gaps in existing test coverage**

2. **Pattern Alignment Strategy**
   - **Follow Existing Conventions**: Match established test structure
   - **Extend Current Patterns**: Build upon existing test utilities
   - **Reuse Test Fixtures**: Leverage existing test data builders
   - **Maintain Consistency**: Use same mocking strategies as existing tests

3. **Spock-Specific Test Planning**
   - Identify data-driven test opportunities
   - Plan where: blocks with meaningful test data
   - Design feature methods with descriptive names
   - Plan shared test fixtures using @Shared
   - **Align with existing Spock usage patterns**

4. **Test Data Strategy**
   Test: "Calculate pricing with various inputs"
   Data points:
   - Regular customer, small order
   - Premium customer, large order
   - New customer, edge case amounts
   - Invalid customer types
   - Null/empty inputs

5. **Mock Strategy Documentation**
   - External services → Always mock
   - Database calls → Mock for unit tests, real for integration
   - Time-dependent code → Use Clock abstraction
   - File system → Use in-memory alternatives
   - **Follow existing project mock patterns**

## Phase 3: IMPLEMENTATION PHASE

### Objectives
- Create well-structured test suites
- Implement maintainable test code
- Establish testing patterns
- **Mirror source code package structure exactly**
- **Extend existing tests where present**

### Steps
1. **Existing Test Enhancement**
   - **Review Current Tests**: Analyze existing Spec files FOR {package_name} ONLY
   - **Identify Missing Scenarios**: Add test cases for uncovered paths IN {package_name}
   - **Refactor If Needed**: Improve existing tests IN {package_name} while maintaining their intent
   - **Preserve Working Tests**: Don't break existing passing tests IN {package_name}
   - **Extract Common Patterns**: Create shared utilities from repeated code IN {package_name} tests

2. **Project Structure Setup - Mirroring Source Packages**
   Create test structure ONLY for {package_name}. For example, if {package_name} is "com.company.project.service":
   ```
   src/
   ├── main/
   │   └── java/
   │       └── com/
   │           └── company/
   │               └── project/
   │                   └── service/          # Focus ONLY on this package
   │                       ├── UserService.java
   │                       └── OrderService.java
   └── test/
       └── groovy/
           └── com/
               └── company/
                   └── project/
                       └── service/      # Create tests ONLY for this package
                           ├── UserServiceSpec.groovy
                           └── OrderServiceSpec.groovy
   ```

3. **Package Structure Rules**
   - **Exact Mirror**: Test packages must exactly match source packages
   - **Naming Convention**: For class UserService.java in com.company.project.service, create UserServiceSpec.groovy in the same package structure
   - **Package-info**: If source has package-info.java, consider test package documentation
   - **Integration Tests**: Add sub-package for integration tests within the same structure
     `test/groovy/com/company/project/service/integration/UserServiceIntegrationSpec.groovy`

4. **Working with Existing Tests**
   When UserServiceSpec.groovy already exists:

   ```groovy
   class UserServiceSpec extends Specification {
       // EXISTING TEST - DO NOT REMOVE
       def "should create user successfully"() {
           // ... existing test code ...
       }

       // NEW TEST - ADDING COVERAGE
       def "should handle null user gracefully"() {
           given: "null user input"
           User user = null

           when: "creating user with null"
           userService.createUser(user)

           then: "appropriate exception is thrown"
           thrown(IllegalArgumentException)
       }

       // Follow same patterns as existing tests above
   }
   ```

5. **Continuous Validation During Implementation**
   After implementing each test class or significant test additions:

   ```
   # ✅ PRIMARY - Run tests using scout-server tools:
   invoke gradle_tester with:
   - projectRoot: "/absolute/path/to/project"
   - session_id: "unique-session-id"

   # ✅ PRIMARY - Check coverage improvements:
   invoke gradle_jacoco_coverage_reporter with:
   - projectRoot: "/absolute/path/to/project"
   - session_id: "unique-session-id"

   # For specialized testing needs (performance, mutation testing),
   # use appropriate alternative tools with documentation
   ```

6. **Spock Test Implementation Pattern with Package Alignment**
   File: `src/test/groovy/com/company/project/service/UserServiceSpec.groovy`

   ```groovy
   package com.company.project.service

   import com.company.project.domain.User
   import com.company.project.repository.UserRepository
   import spock.lang.Specification
   import spock.lang.Subject
   import spock.lang.Title
   import spock.lang.Unroll

   @Title("User Service Tests")
   @Subject(UserService)
   class UserServiceSpec extends Specification {

       def userRepository = Mock(UserRepository)
       def userService = new UserService(userRepository)

       @Unroll
       def "should create user with valid data"() {
           given: "valid user data"
           def user = new User(name: "John", email: "john@example.com")

           when: "creating user"
           def result = userService.createUser(user)

           then: "user is saved"
           1 * userRepository.save(user) >> user
           result == user
       }
   }
   ```

7. **Test Organization Guidelines**
   - **Unit Tests**: Same package as source class
   - **Integration Tests**: Same package with .integration suffix
   - **Test Fixtures**: Create fixtures sub-package within relevant package
   - **Test Utilities**: Mirror util package structure for test utilities
   - **Existing Test Preservation**: Never delete working tests, only enhance

8. **Package-Specific Testing Patterns**
   Domain package tests - focus on business rules:
   ```groovy
   package com.company.project.domain
   class OrderSpec extends Specification {
       def "should calculate total with tax"() {
           // Domain logic tests
       }
   }
   ```

   Service package tests - focus on orchestration:
   ```groovy
   package com.company.project.service
   class OrderServiceSpec extends Specification {
       def "should coordinate order placement"() {
           // Service layer tests with mocks
       }
   }
   ```

   Repository package tests - focus on data access:
   ```groovy
   package com.company.project.repository
   class OrderRepositorySpec extends Specification {
       def "should find orders by customer"() {
           // Repository tests, possibly integration
       }
   }
   ```

9. **Import Management**
   - Tests should import from the same package structure
   - Avoid relative imports
   - Group imports: Spock, Java/Groovy, Project classes, Static imports
   - **Match import style of existing tests**

## Phase 4: EXECUTION PHASE

### Objectives
- Validate test implementation
- Achieve target coverage
- Ensure test stability
- **Verify package structure compliance**
- **Ensure existing tests still pass**

### ⚠️ CRITICAL: Tool Usage for Execution Phase ⚠️
This phase requires frequent test execution. **Use scout-server tools as primary choice for unit tests and Jacoco coverage.**

### Steps
1. **Initial Test Execution**
   ```
   # ✅ PRIMARY - Run all tests using scout-server:
   invoke gradle_tester with:
   - projectRoot: "/absolute/path/to/project"
   - session_id: "unique-session-id"

   # Alternative tools may be used for:
   # - Integration test suites
   # - Performance test execution
   # - Contract testing
   # Document the specific use case when using alternatives
   ```
   - Verify all tests pass
   - Document any failures
   - Check build time

2. **Package Coverage Verification**
   ```
   # ✅ PRIMARY - Generate Jacoco coverage report:
   invoke gradle_jacoco_coverage_reporter with:
   - projectRoot: "/absolute/path/to/project"
   - session_id: "unique-session-id"

   # Alternative coverage tools may be used for:
   # - Mutation coverage analysis
   # - Branch coverage visualization
   # - Language-specific coverage (non-Java)
   ```
   - Review the XML report from scout-server to verify {package_name} coverage
   - Check coverage metrics per package against targets:
     - domain/: 90% target
     - service/: 80% target
     - repository/: 70% target
     - controller/: 75% target
     - util/: 85% target
   - Identify packages below their target thresholds
   - **Compare coverage before and after additions using scout-server reports**

3. **Failure Analysis Protocol**
   If tests fail:
   - Categorize failures by package
   - Document package-specific issues
   - Track cross-package integration failures
   - **Distinguish between existing test failures and new test failures**
   - Re-run specific test suites for debugging using `gradle_tester`

4. **Coverage Measurement by Package**
   Use scout-server coverage reports to track:
   - domain/: 90% (critical business logic)
   - service/: 80% (orchestration layer)
   - repository/: 70% (data access layer)
   - controller/: 75% (API layer)
   - util/: 85% (utility functions)

5. **Iterative Improvement**
   Based on scout-server coverage reports:
   - Identify methods with zero coverage
   - Focus on packages below their target thresholds:
     - Priority 1: Packages with 0% coverage
     - Priority 2: Packages >20% below target (e.g., domain at 65% vs 90% target)
     - Priority 3: Packages 10-20% below target
     - Priority 4: Packages <10% below target
   - Add targeted tests for uncovered branches
   - Re-run coverage reporter after each improvement using `gradle_jacoco_coverage_reporter`
   - Track progress towards coverage goals

## Phase 5: TEST BRITTLENESS VALIDATION PHASE

### Objectives
- Ensure tests actually validate behavior
- Eliminate false-positive tests
- Strengthen test assertions
- **Validate package-level test isolation**
- **Verify existing tests are not brittle**

### Steps
1. **Implementation Mutation Process**
   - Mutate implementations within each package
   - Verify tests in the same package detect mutations
   - Check that tests in other packages aren't affected
   - **Test both new and existing tests for brittleness**

   For each tested class:
   ```java
   // Original
   public int calculate(int input) {
       return input * 2;
   }

   // Mutated (change internal logic)
   public int calculate(int input) {
       return input + input;  // Same result, different implementation
   }

   // Or more aggressive mutation
   public int calculate(int input) {
       return 42;  // Completely wrong
   }
   ```

2. **Mutation Testing Validation**
   After each mutation:
   ```
   # ✅ PRIMARY - Run tests to verify they catch the mutation:
   invoke gradle_tester with:
   - projectRoot: "/absolute/path/to/project"
   - session_id: "unique-session-id"

   # For formal mutation testing tools (PIT, etc.),
   # use appropriate alternative with justification
   ```
   - Ensure appropriate tests fail
   - Document which tests caught the mutation
   - Identify tests that didn't detect the change

3. **Package Isolation Verification**
   ```groovy
   // Ensure service tests don't break when domain internals change
   // Only public API changes should affect cross-package tests
   ```

4. **Test Validation Protocol**
   - Run package-specific test suites after mutations using `gradle_tester`
   - Verify appropriate test failures
   - Ensure no cascading failures across packages
   - **Check that existing tests catch mutations appropriately**

5. **Common Brittleness Issues by Package Type**
   - **Domain Tests**: Over-testing getters/setters
   - **Service Tests**: Testing mock behavior instead of service logic
   - **Repository Tests**: Testing framework behavior
   - **Controller Tests**: Over-mocking service layer

6. **Restoration and Verification**
   - Restore original implementation
   - Run final validation:
   ```
   # ✅ PRIMARY - Ensure all tests pass after restoration:
   invoke gradle_tester with:
   - projectRoot: "/absolute/path/to/project"
   - session_id: "unique-session-id"

   # ✅ PRIMARY - Generate final coverage report:
   invoke gradle_jacoco_coverage_reporter with:
   - projectRoot: "/absolute/path/to/project"
   - session_id: "unique-session-id"
   ```
   - Verify all tests pass
   - **Confirm no regression in existing tests**

## Phase 6: REPORTING PHASE

### Objectives
- Review scout-server coverage reports
- Identify improvement opportunities
- Establish maintenance guidelines
- **Document package-level testing standards**

### Steps
1. **Review Final Coverage Report**
   ```
   # ✅ PRIMARY - Generate comprehensive final coverage report:
   invoke gradle_jacoco_coverage_reporter with:
   - projectRoot: "/absolute/path/to/project"
   - session_id: "unique-session-id"

   # Additional analysis tools may be used for:
   # - Trend analysis
   # - Coverage visualization
   # - Executive dashboards
   ```
   - Review the XML report from scout-server
   - Note coverage by package
   - Compare with initial baseline

2. **Coverage Report Analysis**
   Use the scout-server generated report to identify:
   - Current coverage levels per package
   - Classes with low coverage
   - Methods with zero coverage
   - Branch coverage gaps
   - Comparison with initial coverage baseline

3. **Build Performance Metrics**
   Review metrics from scout-server execution:
   - Total build time
   - Test execution time
   - Number of tests
   - Test stability (flaky test identification)

4. **Package Structure Compliance**
   Based on file system analysis:
   - List any source packages without test packages
   - Identify misaligned test packages
   - Document intentional deviations with justification

5. **Test Organization Guidelines**
   Package Testing Standards:

   1. Every source package must have a corresponding test package
   2. Test class naming: [ClassName]Spec.groovy
   3. Integration tests: [package].integration sub-package
   4. Shared fixtures: [package].fixtures sub-package
   5. Test utilities: Mirror source util package structure
   6. Existing test patterns must be preserved and extended

6. **Maintenance Guidelines by Package**
   - **Domain Package Tests**: Focus on business rules, avoid infrastructure
   - **Service Package Tests**: Mock external dependencies, test orchestration
   - **Repository Package Tests**: Consider in-memory database for speed
   - **Controller Package Tests**: Use MockMvc or similar for endpoint testing


## Additional Guidelines

### Tool Usage Summary
| Task | ✅ Primary Tool | Alternative Use Cases |
|------|----------------|----------------------|
| Unit test execution | `gradle_tester` | Integration test frameworks, performance test runners |
| Jacoco coverage | `gradle_jacoco_coverage_reporter` | Mutation coverage, language-specific coverage tools |
| Build project | `build_gradle_project` | Multi-module builds, non-Gradle projects |
| List dependencies | `dependencies_list` | Security scanning, license compliance tools |

### When to Use Alternative Tools
1. **Integration Testing**: When testing with real databases, message queues, or external services
2. **Performance Testing**: JMeter, Gatling, or similar for load testing
3. **Security Testing**: OWASP tools, dependency vulnerability scanners
4. **Mutation Testing**: PIT or similar for advanced test quality validation
5. **Contract Testing**: Pact or similar for API contract validation
6. **UI Testing**: Selenium, Cypress for frontend testing
7. **Non-JVM Languages**: Language-specific test runners and coverage tools

Always document why an alternative tool was chosen over the primary scout-server tools.

### Package Structure Best Practices
1. **Consistency**: Always mirror source package structure exactly
2. **Discovery**: IDE and build tools can better associate tests with source
3. **Navigation**: Developers can easily find tests for any class
4. **Refactoring**: Package moves in source should trigger test package moves
5. **Coverage**: Tools can accurately report package-level coverage

### Package-Specific Testing Strategies
- **Domain/Model Packages**: Unit tests only, no external dependencies
- **Service Packages**: Mix of unit and integration tests
- **Repository Packages**: Integration tests with test database
- **Controller Packages**: Integration tests with MockMvc
- **Util Packages**: Pure unit tests, high coverage expected

### Working with Existing Tests
1. **Never Delete**: Don't remove working tests, only enhance
2. **Pattern Matching**: Follow established patterns in the codebase
3. **Incremental Improvement**: Add missing scenarios to existing tests
4. **Refactor Carefully**: Improve readability while preserving intent
5. **Document Changes**: Comment why tests were modified

### Scout-Server Tool Usage Best Practices
1. **Regular Validation**: Run `gradle_tester` after each significant change
2. **Coverage Tracking**: Use `gradle_jacoco_coverage_reporter` to monitor progress
3. **CI Integration**: Incorporate both tools into continuous integration pipeline
4. **Performance Monitoring**: Track build times to ensure tests remain fast
5. **Report Analysis**: Use XML coverage reports from scout-server for insights
6. **Session Management**: Always provide a consistent `session_id` for tool calls
7. **Absolute Paths**: Always use absolute paths for `projectRoot` parameter

### Common Mistakes to Avoid
1. ❌ Using shell commands when scout-server tools are appropriate
2. ❌ Not documenting why alternative tools were chosen
3. ❌ Using relative paths instead of absolute paths
4. ❌ Forgetting to provide session_id parameter
5. ❌ Mixing tool types without clear justification

### Success Criteria
- Every source package has a corresponding test package
- Package structure is 100% mirrored
- Tests are easily discoverable by package
- Package-level coverage meets defined targets (as reported by scout-server)
- Clear separation between unit and integration tests by package
- All critical components have test coverage
- No flaky tests in the suite
- Tests run in under 5 minutes (validated by scout-server)
- Clear documentation for maintenance
- Established testing patterns
- Existing tests enhanced and still passing
- Consistent patterns across all tests
- Coverage tracked via scout-server reports
- **Appropriate tool selection with clear documentation of choices**
"""

available_tools = ["filesystem", "shell", "scout-server"]